<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.msa.board.mapper.BoardMapper">

    <select id="getBoardList" resultType="egovMap">
        SELECT
            *
        FROM public.board
        WHERE use_yn = 'Y'
        <if test="userId != null and userId != ''">
            AND user_id = #{userId}
        </if>
        ORDER BY "board_id" DESC
    </select>

    <select id="getBoardById" resultType="egovMap">
        SELECT
            board_id,
            user_id,
            title,
            content,
            view_cnt,
            use_yn,
            reg_ip,
            created_dt,
            updated_dt
        FROM public.board
        WHERE board_id = #{boardId}
        AND use_yn = 'Y'
    </select>

    <!-- userId는 현재 사용하지 않지만 추후 확장 가능 -->

</mapper>